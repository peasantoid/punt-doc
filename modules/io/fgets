string (fgets
         file:stream
         number:count)

If .count >= 0, read .count bytes from .stream and return as string.
Otherwise, attempt to read all bytes.
If unsuccessful, return false.

Bug: Behavior when reading all bytes from standard input may be unpredictable.
