Logical operators.
Any non-nil value is considered true.

bool (not
       a) -> a == false
bool (true
       a) -> a == true

bool (and
       a
       b) -> a && b
bool (or
       a
       b) -> a || b

bool (yes) -> true
bool (no)  -> false
